# 主项目性能工具只在显式开启 benchmark 时构建。
if(NOT PRISM_ENABLE_BENCHMARK)
    message(STATUS "Main performance tools disabled (PRISM_ENABLE_BENCHMARK=OFF)")
    return()
endif()

# 主项目性能基准（prism 静态库）
add_executable(BlackholeServer BlackholeServer.cpp)
target_compile_options(BlackholeServer PRIVATE -g1 -Os)
target_link_libraries(BlackholeServer PRIVATE PrismStaticLibrary)
target_include_directories(BlackholeServer BEFORE PRIVATE ${CMAKE_SOURCE_DIR}/tests ${CMAKE_SOURCE_DIR}/src)
add_executable(EncryptParallel EncryptParallel.cpp)
target_compile_options(EncryptParallel PRIVATE -g1 -Os)
target_link_libraries(EncryptParallel PRIVATE PrismStaticLibrary)
target_include_directories(EncryptParallel BEFORE PRIVATE ${CMAKE_SOURCE_DIR}/tests ${CMAKE_SOURCE_DIR}/src)
add_executable(FileServer FileServer.cpp)
target_compile_options(FileServer PRIVATE -g1 -Os)
target_link_libraries(FileServer PRIVATE PrismStaticLibrary)
target_include_directories(FileServer BEFORE PRIVATE ${CMAKE_SOURCE_DIR}/tests ${CMAKE_SOURCE_DIR}/src)
add_executable(ProtocolPerf ProtocolPerf.cpp)
target_compile_options(ProtocolPerf PRIVATE -g1 -Os)
target_link_libraries(ProtocolPerf PRIVATE PrismStaticLibrary)
target_include_directories(ProtocolPerf BEFORE PRIVATE ${CMAKE_SOURCE_DIR}/tests ${CMAKE_SOURCE_DIR}/src)
add_executable(TcpBlockMatrix TcpBlockMatrix.cpp)
target_compile_options(TcpBlockMatrix PRIVATE -g1 -Os)
target_link_libraries(TcpBlockMatrix PRIVATE PrismStaticLibrary)
target_include_directories(TcpBlockMatrix BEFORE PRIVATE ${CMAKE_SOURCE_DIR}/tests ${CMAKE_SOURCE_DIR}/src)
