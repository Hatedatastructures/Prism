AddProductionTest(TransportLayer.cpp)
AddProductionTest(QuicE2E.cpp)
if (CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    if (CMAKE_BUILD_TYPE STREQUAL "Debug")
        target_compile_options(QuicE2E PRIVATE -U_FORTIFY_SOURCE)
    else()
        target_compile_options(QuicE2E PRIVATE -O3)
    endif()
endif()
