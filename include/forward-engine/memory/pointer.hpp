/**
 * @file pointer.hpp
 * @brief 智能指针定义 (预留)
 * @details 当前暂未使用，保留作为未来扩展智能指针别名的占位符。
 */
#pragma once


// namespace ngx::memory
// {
//     template <typename T>
//     using unique_ptr = std::unique_ptr<T>;
// }
