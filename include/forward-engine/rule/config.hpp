/**
 * @file config.hpp
 * @brief 规则配置结构
 * @details 定义规则模块的配置结构，用于配置规则引擎的行为参数。
 * 该模块为规则引擎提供可配置的选项，支持灵活的安全策略定制。
 * 设计目标包括可扩展性、热重载和类型安全。可扩展性方面，配置
 * 结构设计为可扩展，便于添加新的规则配置项。热重载方面，支持
 * 运行时配置更新，无需重启服务。类型安全方面，使用强类型配置
 * 项，避免配置错误。配置项预留包括黑名单文件路径指定黑名单
 * 数据文件的加载路径，规则更新间隔配置规则热重载的时间间隔，
 * 匹配模式配置域名匹配的精确度（精确匹配或后缀匹配）。
 * @note 当前配置结构为空，预留用于未来扩展规则相关的配置项。
 * @warning 配置修改后需要重新加载才能生效。
 */
#pragma once

#include <string>
#include <cstdint>

/**
 * @namespace ngx::rule
 * @brief 规则引擎模块
 * @details 定义了 ForwardEngine 的规则引擎组件，包括黑名单、访问
 * 控制等规则管理功能。设计原理包括规则抽象、高性能匹配和线程
 * 安全。规则抽象将访问控制、安全过滤等规则抽象为统一的规则接口。
 * 高性能匹配使用高效的数据结构和算法实现快速规则匹配。线程安全
 * 方面，规则查询操作设计为线程安全，支持多线程并发访问。包含
 * 组件包括黑名单 blacklist 基于 IP 和域名的黑名单管理，配置管理
 * config 规则引擎配置结构，规则链为未来可扩展规则链和组合规则。
 * 使用准则方面，规则匹配应在热路径中保持高效避免复杂计算，规则
 * 更新应支持热重载无需重启服务，规则引擎应提供详细的匹配日志
 * 便于调试和审计。
 * @note 规则引擎应保持无状态，规则数据通过配置加载。
 * @warning 规则匹配可能影响性能，应避免过于复杂的规则逻辑。
 */
namespace ngx::rule
{
    /**
     * @struct config
     * @brief 规则配置结构体
     * @details 目前为空，预留用于未来扩展规则相关的配置项。
     * 计划支持的配置项包括黑名单文件路径、规则更新间隔、
     * 匹配模式等。
     * @note 配置结构设计为可扩展，后续可根据需求添加字段。
     */
    struct config
    {

    };
}
