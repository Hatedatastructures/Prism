/**
 * @file config.hpp
 * @brief 规则配置
 * @details 定义规则模块的配置结构。
 */
#pragma once

#include <string>
#include <cstdint>

namespace ngx::rule
{
    /**
     * @brief 规则配置结构体
     * @details 目前为空，预留用于未来扩展规则相关的配置项。
     */
    struct config
    {

    };
}