cmake_minimum_required(VERSION 3.15)

project(forward_engine_concurrency_test LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED YES)
set(CMAKE_CXX_EXTENSIONS NO)

add_executable(High_performance_backend_server
    server.cpp
    server.hpp
    conversation.hpp
    handler.hpp
)

target_include_directories(High_performance_backend_server PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/include
    "c:/bin/include"
)

target_link_libraries(High_performance_backend_server PRIVATE
    Forward_static_library
    Boost::system
    glaze::glaze
    spdlog::spdlog
    ws2_32
    crypt32
    mswsock
)

if(MSVC)
    target_compile_options(High_performance_backend_server PRIVATE /W4 /O2 /DNDEBUG)
else()
    target_compile_options(High_performance_backend_server PRIVATE -Wall -Wextra -O2 -DNDEBUG)
endif()

add_executable(client
    client.cpp
)

target_include_directories(client PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/include
    "c:/bin/include"
)

target_link_libraries(client PRIVATE
    Forward_static_library
    Boost::system
    glaze::glaze
    spdlog::spdlog
    ws2_32
    crypt32
    mswsock
)

if(MSVC)
    target_compile_options(client PRIVATE /W4 /O2 /DNDEBUG)
else()
    target_compile_options(client PRIVATE -Wall -Wextra -O2 -DNDEBUG)
endif()
